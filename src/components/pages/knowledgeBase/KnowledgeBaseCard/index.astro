---
import './index.scss';

const { label, icon } = Astro.props;

import { Image } from 'astro:assets';
---

<div class="knowledge-base-card">
  <p class="knowledge-base-card__label">{label}</p>

  <Image src={icon} alt={label} />
</div>
