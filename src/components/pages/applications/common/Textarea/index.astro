---
import './_index.scss';

interface Props {
  rows?: number;
  placeholder?: string;
}

const { rows = 4, placeholder } = Astro.props;
---

<textarea rows={rows} placeholder={placeholder}></textarea>
